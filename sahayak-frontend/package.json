{
  "name": "sahayak-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/",
    "start": "serve -s dist"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
